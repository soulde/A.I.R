CREATE TABLE names (
    ID         STRING PRIMARY KEY
                      NOT NULL,
    Name       STRING NOT NULL,
    Password   STRING,
    Tel        STRING,
    QQ         STRING,
    Sex        STRING,
    TechGroup  STRING NOT NULL
                      DEFAULT 无,
    School     STRING,
    AdminGroup STRING NOT NULL
                      DEFAULT 无,
    Grade         STRING
);
